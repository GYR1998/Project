这是一个共享目录服务器，它的功能就是可以展示文件信息，然后可以供用户上传、下载文件等。
因为我对制作网页的一些语言不是很了解，所以用户的网页很粗糙，不是很美观，但是能够实现主要的功能。

如果想要验证功能的话，可以打开Linuk，在其中输入下面两行代码：
systemctl stop firewalld
sudo netstat -anptu|grep 9000
然后再浏览器中输入本机的ip地址和端口号（端口号为9000，当然自己也可以在代码中改）然后回车就可以了。

注意：在使用该代码前，应该在同层次的目录下新创建一个www目录，这个www目录中存放的文件就是在网页上显示的文件。

